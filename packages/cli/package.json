{
  "name": "@brrock/patchsync",
  "version": "0.1.1",
  "private": false,
  "files":["bin"],
  "type": "module",
  "bin": {
    "patchsync": "dist/main.js"
  },
  "scripts": {
    "build": "bun build src/main.ts --target bun --outdir dist --minify --banner '#!/usr/bin/env bun'"
  },
  "devDependencies": {
    "@brrock/patchsync-action": "workspace:*"
  }
}
