{
  "name": "@brrock/patchsync-action",
  "version": "0.2.0",
  "private": false,
  "files": ["dist"],
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "build": "bun build src/main.ts --target bun --outdir dist --minify --sourcemap=none",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "minimatch": "^10.0.1",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  },
  "bin": {
    "patchsync-action": "dist/main.js"
  }
}
